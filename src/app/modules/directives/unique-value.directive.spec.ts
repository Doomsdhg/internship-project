import { UniqueValueDirective } from './unique-value.directive';

describe('UniqueValueDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueValueDirective();
    expect(directive).toBeTruthy();
  });
});
