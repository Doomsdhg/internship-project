import { NumericLengthDirective } from './numeric-length.directive';

describe('NumericLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericLengthDirective();
    expect(directive).toBeTruthy();
  });
});
