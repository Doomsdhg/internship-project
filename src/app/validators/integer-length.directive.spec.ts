import { IntegerLengthDirective } from './integer-length.directive';

describe('IntegerLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerLengthDirective();
    expect(directive).toBeTruthy();
  });
});
